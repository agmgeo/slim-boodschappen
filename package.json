{
  "name": "slimme-boodschappen",
  "private": true,
  "version": "0.1.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev:api": "pnpm --filter @app/api start:dev",
    "dev:web": "pnpm --filter @app/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@9.0.0"
}
